# Copy this file to `.env` (in the same directory) and fill in your real values.
# `.env` is gitignored — never commit it.
# Strava credentials come from https://www.strava.com/settings/api

STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
